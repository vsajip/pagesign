name: Tests

on:
  push:
    branches: [ main ]
    paths-ignore:
      - 'LICENSE.*'
      - 'README.*'
      - '.github/ISSUE-TEMPLATE/**'
      - 'docs/**'
      - '.hgignore'
      - '.gitignore'

  pull_request:
    branches: [ main ]
    paths-ignore:
      - 'LICENSE.*'
      - 'README.*'
      - '.github/ISSUE-TEMPLATE/**'
      - 'docs/**'
      - '.hgignore'
      - '.gitignore'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.7', '3.8', '3.9', '3.10', '3.11-dev', 'pypy-3.9']

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    #- name: Set up age (POSIX)
      #if: ${{ matrix.os != 'windows-latest' }}
      #uses: adnsio/setup-age-action@v1.1.0
      #with:
        #version: ^1.0.0
    - name: Set up age and minisign (POSIX)
      if: ${{ matrix.os != 'windows-latest' }}
      run: |
        brew install age minisign
    - name: Set up age and minisign (Windows)
      if: ${{ matrix.os == 'windows-latest' }}
      run: |
        choco install age.portable
        choco install minisign
    - name: Test with unittest
      run: |
        age --version
        minisign -v
        python test_pagesign.py
    - name: Test with coverage
      run: |
        pip install coverage
        coverage run --branch test_pagesign.py
        coverage xml
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        flags: unittests
        files: coverage.xml
        fail_ci_if_error: true
